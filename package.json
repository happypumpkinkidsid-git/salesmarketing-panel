{
  "name": "hp-dashboards",
  "version": "0.1.0",
  "private": true,
  "description": "Happy Pumpkin web dashboards monorepo (KOL Command Center + sales + creative hub)",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod --build",
    "sync": "/tmp/kol-venv/bin/python scripts/sync-sheets.py"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.2",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
